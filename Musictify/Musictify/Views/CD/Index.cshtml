@model IEnumerable<Musictify.Models.CD>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CDName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CDPrice)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CDImageURL)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CDName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CDPrice)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CDImageURL)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CDID" class="text-info">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CDID" class="text-info">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CDID" class="text-info">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
