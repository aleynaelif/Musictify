@model Musictify.Models.Concert

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Concert</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ConcertID" />
            <div class="form-group">
                <label asp-for="StadiumID" class="control-label"></label>
                <select asp-for="StadiumID" class="form-control" asp-items="ViewBag.StadiumID"></select>
                <span asp-validation-for="StadiumID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ConcertDate" class="control-label"></label>
                <input asp-for="ConcertDate" class="form-control" />
                <span asp-validation-for="ConcertDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
