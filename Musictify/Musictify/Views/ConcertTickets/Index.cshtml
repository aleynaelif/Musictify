@model IEnumerable<Musictify.Models.ConcertTickets>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table text-white">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Concert)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tickets)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Concert.ConcertID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tickets.TicketsID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ConcertTicketsID" class="text-info">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ConcertTicketsID" class="text-info">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ConcertTicketsID" class="text-info">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
