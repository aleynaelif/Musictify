@model IEnumerable<Musictify.Models.SingerAlbum>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Album)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Singer)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Album.AlbumID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Singer.SingerID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID" class="text-info">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID" class="text-info">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID" class="text-info">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
