@model IEnumerable<Musictify.Models.Tickets>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PurchaseDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TicketPricing)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SeatRow)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SeatColumn)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PurchaseDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TicketPricing.TicketPricingID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SeatRow)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SeatColumn)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TicketsID" class="text-info">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TicketsID" class="text-info">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TicketsID" class="text-info">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
