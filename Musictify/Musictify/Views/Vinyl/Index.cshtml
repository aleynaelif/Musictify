@model IEnumerable<Musictify.Models.Vinyl>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table text-white">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.VinylName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VinylPrice)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VinylImageURL)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.VinylName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.VinylPrice)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.VinylImageURL)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.VinylID" class="text-info">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.VinylID" class="text-info">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.VinylID" class="text-info">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
